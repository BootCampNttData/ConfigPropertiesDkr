server:
  port: 8761

#Eureka Configuration Properties
eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false
    service-url:
      defaultZone: http://192.168.0.121:${server.port}/eureka/
#
#    fetch-registry: false
#    register-with-eureka: false
#    service-url:
#      default-zone: http://192.168.0.121:${server.port}/eureka/

#      default-zone: http://host.docker.internal:${server.port}/eureka/